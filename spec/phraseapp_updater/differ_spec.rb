require 'spec_helper'

describe PhraseappUpdater do
  it 'has a version number' do
    expect(PhraseappUpdater::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
